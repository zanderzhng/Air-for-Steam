resource/layout/uistatuspanel.layout {

	styles {
		CUIStatusPanel {
		}

		SlimProgressBar {
			textcolor=none
			bgcolor=none
			render {
				0="image( x0, y0 - 78, x1, y1, graphics/material/download )"
			}
			render_bg {
				//can use for background that only shows up during active downloads
				//!! text does not appear for workshop downloads leaving only the bg, don't use
			}
		}

		StatusLabel { textcolor=none }
		StatusLabelDim { textcolor=none }
	}

	layout {
		place { control=StatusProgressbar align=bottom width=max height=0 }
		place { control=StatusDownloading width=max height=max }

		place { control=StatusPaused,StatusComplete height=0 width=0 }
	}
}
